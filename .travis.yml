language: php
sudo: false
cache:
    directories:
        - $HOME/.composer/cache/files
        - $HOME/symfony-bridge/.phpunit

        
git:
  clone: true

env:
    global:
        - PHPUNIT_FLAGS="-v"
        - SYMFONY_PHPUNIT_DIR="$HOME/symfony-bridge/.phpunit"
        - COMPOSER_MEMORY_LIMIT=-1

matrix:
    fast_finish: true
    include:
          # Test the latest stable release
        - php: 7.2
        - php: 7.3
          env: DEPENDENCIES="symfony/website-skeleton" STABILITY="stable"

          # Test LTS versions. This makes sure we do not use Symfony packages with version greater
          # than 3 or 4 respectively. Read more at https://github.com/symfony/lts
        - php: 7.1
        - php: 7.2
        - php: 7.3
          env: 
              - DEPENDENCIES="symfony/lts:^3"
              - DEPENDENCIES="symfony/lts:^4"

          # Latest commit to master
        - php: 7.3
          env: STABILITY="dev"

    allow_failures:
          # Dev-master is allowed to fail.
        - env: STABILITY="dev"

before_install:
    - if [[ $COVERAGE != true ]]; then phpenv config-rm xdebug.ini || true; fi
    - if ! [ -z "$STABILITY" ]; then composer config minimum-stability ${STABILITY}; fi;
    - if ! [ -v "$DEPENDENCIES" ]; then composer require --no-update ${DEPENDENCIES}; fi;
    - echo $TRAVIS_BRANCH
    - mkdir -p lib/xact/js-routing
    - ls -al
    - cd lib/xact/js-routing

install:
    # To be removed when this issue will be resolved: https://github.com/composer/composer/issues/5355
    - if [[ "$COMPOSER_FLAGS" == *"--prefer-lowest"* ]]; then composer update --prefer-dist --no-interaction --prefer-stable --quiet; fi
    - composer update ${COMPOSER_FLAGS} --prefer-dist --no-interaction
    - ./vendor/bin/simple-phpunit install

before_script:
    - mkdir lib/xact/js-routing

script:
    - composer validate --strict --no-check-lock
    # simple-phpunit is the PHPUnit wrapper provided by the PHPUnit Bridge component and
    # it helps with testing legacy code and deprecations (composer require symfony/phpunit-bridge)
    - ./vendor/bin/simple-phpunit $PHPUNIT_FLAGS
