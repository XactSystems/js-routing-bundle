language: php
sudo: false
cache:
    directories:
        - $HOME/.composer/cache/files
        - $HOME/symfony-bridge/.phpunit

        
git:
  clone: false

env:
    global:
        - PHPUNIT_FLAGS="-v"
        - SYMFONY_PHPUNIT_DIR="$HOME/symfony-bridge/.phpunit"
        - COMPOSER_MEMORY_LIMIT=-1

matrix:
    fast_finish: true
    include:
          # Test the 3.4 LTS release
        - php: 7.2
          env: SYMFONY="3.4"
        - php: 7.3
          env: SYMFONY="3.4"

before_install:
    - if [[ $COVERAGE != true ]]; then phpenv config-rm xdebug.ini || true; fi
    - composer create-project symfony/website-skeleton:$SYMFONY symfony
    - cd symfony
    - composer config repositories.xactsystems vcs https://github.com/XactSystems/js-routing-bundle.git --no-interaction
    # Does not work for private repos, needs SSH key or other auth
    - composer require xactsystems/js-routing-bundle:dev-3.4 --no-interaction
    - cat vendor/composer/autoload_psr4.php

install:
    - ls -al vendor/xactsystems
    - cp -p vendor/xactsystems/js-routing-bundle/phpunit.xml.dist .
    - ./vendor/bin/simple-phpunit install

script:
    #- composer validate --strict --no-check-lock
    # simple-phpunit is the PHPUnit wrapper provided by the PHPUnit Bridge component and
    # it helps with testing legacy code and deprecations (composer require symfony/phpunit-bridge)
    - ./vendor/bin/simple-phpunit $PHPUNIT_FLAGS
